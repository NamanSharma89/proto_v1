As an expert python AI engineer, help me analyse the attached code base.

I have introduced a new module named hospital-data-chatbot-etl which would hold all the current logic of excel data load into database. Since the current code is not cloud compatible, I need to introduce configs which would help me run this new module in local and in cloud. 

First step, help me generate necessary files for new module. Recommend which files I need to copy from existing module so there are no deviations from project standard.

We will continue once the first step is over. Work with me as an agent on this problem. Keep a constant memory of the steps we take.

Take note of the already generated files and review the mistakes. Lets correct those first.